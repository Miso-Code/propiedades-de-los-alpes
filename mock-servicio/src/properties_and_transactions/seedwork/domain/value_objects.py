"""Reusable value objects part of the project's seedwork

In this file, you will find the reusable value objects that are part of the project's seedwork.

"""

from dataclasses import dataclass

@dataclass(frozen=True)
class ValueObject:
    ...